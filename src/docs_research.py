"""批量生成 research/（研究文档）+ 自检清单 + GTM 执行材料。数值均从 market.json 读取。"""
from __future__ import annotations

import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from common import ROOT, SOURCES, log_line                            # noqa: E402

M = json.loads((ROOT / "github-upload" / "data" / "market.json")
               .read_text(encoding="utf-8"))
FX, GX = M["finance"]["derived"], M["gtm"]["derived"]
CUR, MM = FX["current"], FX["money_market"]
DUR = {r["shift_bp"]: r for r in FX["duration_scenario"]["rows"]}
ST = FX["stages"]
BV, BY = GX["byd"]["month"], GX["byd"]["ytd"]
CH, PM, CMP = GX["channels"], GX["powertrain_mix"], GX["competitors"]
DV = M["meta"]["data_version"]
GEN = M["meta"]["generated_at"]
F: dict[str, str] = {}

# ============================================================ interview 07
F["interview/07_self_check_list.md"] = f"""# 必须由我本人完成的清单

数据版本 {DV}。**做完一项，把 [ ] 改成 [x]，并写上日期和你发现的问题。**

> 为什么单独列这张表：项目里有几件事 AI 做了不等于我做了。
> 面试里被追问「你核验过吗」时，只有我真的做过才敢说做过。

## A. 数据核验（必须做，占信任度最高）

- [ ] **A1 抽查 3 条固收数据原文**
      打开中国债券信息网，核对当日国债 10Y 是否等于 {CUR['y10']}%（注意工作日与发布日期）。
      打开中国货币网，核对 DR007 是否等于 {MM['DR007']}%。
      记录：核对日期 ______，是否一致 ______，差异原因 ______
- [ ] **A2 抽查 2 条 SMMT 数据**
      打开 `https://www.smmt.co.uk/vehicle-data/car-registrations/`，
      核对纯电当月注册与份额是否等于 {GX['bev_month']:,.0f} 辆 / {PM[0]['share']}%；
      核对车队与私人占比是否等于 {CH[0]['share']}% / {CH[1]['share']}%。
      记录：核对日期 ______，是否一致 ______
- [ ] **A3 核对竞品官网规格**
      逐一打开 {', '.join(c['model'] for c in CMP)} 的英国官网页，
      核对 `market.json` 里的续航数值是否仍然一致（页面可能改版）。
      记录：发现的不一致 ______

## B. 计算复核（证明我真的理解公式）

- [ ] **B1 手算久期情景**
      用计算器手算：D=5.0、C=30、dy=+0.0025（+25bp）、dy=+0.0100（+100bp）。
      上限：-5.0×0.0025 + 0.5×30×0.0025² = ______
      程序输出：{DUR[25]['approx_pct']:+.2f}%
      是否一致 ______；若不一致，原因 ______
- [ ] **B2 手算期限利差**
      {CUR['y10']} − {CUR['y2']} = ______；程序输出 {CUR['spread_10y2y']}。
      是否一致 ______
- [ ] **B3 解释凸性项**
      用你自己的话（不看文档）写出「为什么凸性项恒为正、为什么它让损失变小」，
      写在这里：______________________

## C. 人工判断（AI 不能替代）

- [ ] **C1 复核达人候选清单**
      打开 `research/gtm/influencer_screening_method.md` 里的候选，
      逐个人工确认：作品主题是否真的相关、地区证据是否成立、是否公开可见。
      记录：剔除几个 ______，原因 ______
- [ ] **C2 挑战一条 AI 结论**
      从 `market.json` 的 `claims` 里挑一条（建议 K-07「车队优先」），
      写出你会怎么反驳它：______________________
- [ ] **C3 确认实习百分比的口径**
      京东「交付周期缩短 30% / 执行力提升 20%」、诺安「销售效环比 15%」、
      国学社「阅读量提升 35%」、志愿者「效率提升 40%」——
      这几个数的分母、区间、统计方式核实清楚了吗？
      记录：可保留的 ______，需删除的 ______

## D. 真实用户测试（可选但强烈建议）

- [ ] **D1 邀请 2–3 位读者完成三项查找任务**
      任务一：找到我的背景（限时 30 秒）
      任务二：说出项目 B 的一个结论
      任务三：定位「BYD 价格为未获取」这条证据
      记录：参与者是否属于目标人群（HR / 业务面试官）______
      招募方式 ______；样本限制 ______；发现的问题 ______
      **未做则标注「未测试」，不要写「已验证可用」。**

## E. 交付物检查

- [ ] **E1 在 PowerPoint 或 WPS 里打开 PPTX 逐页看一遍**
      （本机没有 LibreOffice / PowerPoint，无法原生渲染成图片，
      只做了结构检查：18 项通过，含文字溢出测算与数值一致性。）
      记录：发现的问题 ______
- [ ] **E2 在实际打印机上试打打印版 PDF**
      屏幕检查已通过（14 页、0 溢出、无缺字），但**纸面实打未确认**。
      记录：纸张 ______，灰度可辨性 ______，边距是否安全 ______
- [ ] **E3 解压 zip 独立验证**
      在另一个目录解压，确认 `github-upload/` 可以独立打开并显示数据。
      记录 ______

## F. 自动化验证

- [ ] **F1 确认定时任务真的触发过一次**
      查看 `operations/RUN_CONFIG.md`，确认任务标识与最近一次运行状态。
      **只有拿到执行证据后，才能对外说「自动化已生效」。**
      记录：任务标识 ______，最近成功运行 ______

---

## 完成后同步更新以下位置

| 需要更新的地方 | 改成什么 |
|---|---|
| `market.json` → `claims` 中 K-01/K-05 的 exception | 「已由本人抽查原文核对（日期）」 |
| `checks/validation.json` | 增加「人工复核」条目 |
| 网站 `evidence.html` 的人工复核行 | 由「待完成」改为「已完成（日期）」 |
| 简历与面试稿 | 可以把「待完成」从标注里去掉 |
"""
# ============================================================ research/finance
F["research/finance/01_method_and_sources.md"] = f"""# 项目 A 研究方法与来源

数据版本 {DV}　生成 {GEN}

## 1. 研究问题

在收益率中枢长期下移的环境中，久期带来的收益空间在缩小，
但久期风险的绝对敞口并没有同步下降。要回答的是：
当市场出现上行冲击时，不同约束（久期上限、回撤容忍、会计分类）的机构客户
受到的冲击差别有多大，以及应该用什么语言把这个差别讲清楚。

## 2. 核心指标集（刻意收窄）

| 指标 | 数据集编号 | 来源 | 频率 |
|---|---|---|---|
| 国债收益率曲线关键期限（1/2/3/5/7/10/30Y） | D-FIN-YC-* | S-CB-01 | 交易日 |
| 10Y-2Y 期限利差 | D-FIN-SPREAD-10Y2Y | 本项目计算 | 交易日 |
| DR001 / DR007（FDR 系列） | D-FIN-DR001 / D-FIN-DR007 | S-CM-01 | 交易日 |
| FR007（全市场，对照） | D-FIN-FR007 | S-CM-01 | 交易日 |
| Shibor 1W（报价利率旁证） | D-FIN-SHIBOR-1W | S-CM-02 | 交易日 |
| 公开市场业务交易公告 | D-FIN-OMO-LATEST | S-PB-01 | 按公告 |

**为什么只有这些**：宁可少而可核实，不要用一堆无法追溯的口径堆出「完整感」。

## 3. 来源登记

{chr(10).join(f"- **{k}**｜{v['name']}｜权威性 {v['authority']}｜{v['url']}" for k, v in SOURCES.items() if v['region'] == 'CN')}

## 4. 实测得到的接口（公开文档未给出）

| 用途 | 方式 | 参数要点 |
|---|---|---|
| 最新国债曲线 | POST `yield.chinabond.com.cn/cbweb-czb-web/czb/czbChartSearch` | 无参数，返回列表，取 `ycDefName` 含「国债收益率曲线」的一条 |
| 指定工作日国债曲线 | GET `.../czbQueryXy` | `zblx=xy&workTime=YYYY-MM-DD&qxmc=1` |
| 银行间质押式回购利率 | GET `chinamoney.com.cn/ags/ms/cm-u-bk-currency/FrrHis` | 数值在 `records[].frValueMap`，键为 FR001/FR007/FDR001/FDR007 等，值为字符串 |
| Shibor | GET `.../cm-u-bk-shibor/ShiborHis` | 数值在 `records[]` 上，期限即键（`"1W"`），日期在 `showDateCN` |

**踩过的坑（已修正）**
1. `qxmc=2` 返回的是**地方政府债**曲线，不是国债 → 用 `qxmc=1`。
   若不修正，会把两条不同曲线拼在一起，利差全错。
2. `FrrHis` 的数值不在 `lfiFrValue` 字段（该字段为 null），而在 `frValueMap`。
3. 本机存在本地代理（127.0.0.1:64265），对回环与部分外网请求间歇返回 502；
   程序增加 curl 回退通道与重试后才稳定。

## 5. 计算与假设

### 5.1 期限利差
同一条曲线、同一天：`spread = y(10Y) - y(2Y)`。
只允许同曲线同源相减，跨曲线（国债 vs 国开债 vs 地方债）不拼接。

### 5.2 修正久期情景
```
dP/P ≈ -D_mod * dy + 0.5 * C * dy²
```
- `dy` 用小数：**1bp = 0.0001**，+25bp → +0.0025
- 教学假设参数：面值 100,000,000 元、`D_mod` = 5.0、凸性 `C` = 30
- 移位类型：**平行移位**
- 误差来源：①忽略三阶及以上项；②平行移位假设；
  ③未含信用利差变动、税收与交易成本；④未考虑现金流时点变化
- 误差量级：凸性项在 ±25bp 内约占久期效应 6%，±100bp 内升至约 24%

当前结果：
| 移位 | 久期效应 | 凸性效应 | 合计 | 估值变动 |
|---|---|---|---|---|
{chr(10).join(f"| {r['shift_bp']:+d}bp | {r['duration_effect_pct']:+.4f}% | {r['convexity_effect_pct']:+.4f}% | {r['approx_pct']:+.4f}% | {r['value_change_cny']:+,.0f} 元 |" for r in FX['duration_scenario']['rows'])}

## 6. 不得混用清单

| 不要混用 | 原因 |
|---|---|
| 期限 vs 久期 | 期限是到期时间；久期是价格敏感度 |
| 到期收益率 vs 持有期回报 | 后者含票息与再投资 |
| 价格变化 vs 总回报 | 无可靠总回报数据时，不得把收益率变动当回测收益 |
| 国债曲线 vs 地方政府债曲线 | 绝对水平与利差不同 |
| DR(存款类机构) vs FR(全市场) | 参与机构范围不同 |
| Shibor vs 回购成交利率 | 报价均值 vs 实际成交加权 |
| 货币基金七日年化 vs 债券基金区间收益 | 口径不同，不可直接排名 |

## 7. 失效条件

{chr(10).join(f'{i+1}. {x}' for i, x in enumerate(FX['falsifiers']))}
"""

F["research/finance/02_60s_brief_script.md"] = f"""# 每日简报 · 60 秒口述稿

数据版本 {DV}。**口述稿是模板**，实际使用时按当日数据替换方括号内容。

---

## 结构（五段，掐表约 60 秒）

### 0–10 秒｜一句话结论
> 各位，今天曲线和资金面的主要变化是【方向 + 幅度】，
> 对久期敞口的含义是【偏正 / 中性 / 偏负】。

### 10–30 秒｜事实（只说当日的）
> 国债曲线：10Y 在 {CUR['y10']}% 附近，2Y 在 {CUR['y2']}%，
> 10Y-2Y 利差 {CUR['spread_10y2y']} 个百分点。
> 资金面：DR007 {MM['DR007']}%、DR001 {MM['DR001']}%，
> 全市场 FR007 {MM['FR007']}%——注意这两个不是同一个口径，我分开报。
> 公开市场：【当日公告标题与投放/到期，两侧同时核对】。

### 30–45 秒｜可能影响
> 如果曲线维持这个形态，久期较长的组合在估值上【受益 / 承压】；
> 但更需要注意的是：**收益率越往下，可继续下行的空间越有限，
> 而久期敞口并没有因此变小**。所以防御性调整的价值在上升。

### 45–55 秒｜不确定性与口径说明
> 以上数据来自中债、中国货币网与人民银行公告，都是单一权威来源，
> 还没有独立交叉验证。曲线取数日是 {CUR['date']}，如果跨了周末或假期，
> 请注意这不是当日数据。

### 55–60 秒｜下一步
> 需要向您确认的是：您这边的久期上限和回撤容忍度分别是多少？
> 我按您的约束把情景算出来，比直接给观点有用。

---

## 使用纪律

1. **事实与解释分开说。** 报数字时说「数据」；说影响时说「我的判断」。
2. **不同口径不合并。** DR 与 FR 分别报，不取平均。
3. **不报没有的数。** 净投放没算出来就不报；缺数据说「未获取」，不说「约等于」。
4. **口径提醒要主动说。** 「曲线取数日」和「频率」每次都要交代。
5. **60 秒到了就停。** 把追问空间留给对方。
"""

F["research/finance/03_client_comm_onepager.md"] = f"""# 一页机构客户沟通材料（模板）

数据版本 {DV}。**客户案例为教学假设，不代表任何真实机构。**

---

## 标题
**在您的约束下，当前利率情景意味着多少回撤**

## 一、先确认您的约束（不确认就不给结论）

| 约束 | 您的取值 | 为什么必须知道 |
|---|---|---|
| 修正久期上限 | ______ 年 | 决定同一冲击下的估值弹性 |
| 最大可接受回撤 | ______ % / ______ 元 | 决定情景是否已经在红线外 |
| 会计分类 | 摊余成本 / 其他综合收益 / 交易性 | 决定浮亏是否进损益 |
| 流动性要求 | ______ | 决定能否扛过冲击而不是被动卖出 |
| 可用的对冲工具 | ______ | 决定是「承受」还是「调整」 |

> **不能只凭机构类别推断适配性。** 「券商资管」和「城商行理财子」
> 内部差异可能比两个类别之间的差异更大。

## 二、当前事实（含来源与日期）

| 项目 | 数值 | 来源编号 | 数据日期 | 验证状态 |
|---|---|---|---|---|
| 国债 10Y | {CUR['y10']}% | S-CB-01 | {CUR['date']} | 单源权威，未交叉验证 |
| 国债 2Y | {CUR['y2']}% | S-CB-01 | {CUR['date']} | 单源权威，未交叉验证 |
| 10Y-2Y 利差 | {CUR['spread_10y2y']} pp | 本项目计算 | {CUR['date']} | 计算值 |
| DR007 | {MM['DR007']}% | S-CM-01 | {MM['date']} | 单源权威，未交叉验证 |
| FR007 | {MM['FR007']}% | S-CM-01 | {MM['date']} | 单源权威，未交叉验证 |

## 三、情景：对「您的」组合意味着什么

把您的 $D_{{mod}}$ 与组合市值代入 `dP/P ≈ -D_mod·dy + 0.5·C·dy²`：

| 收益率平行移位 | 估值变动（近似） | 折合金额 | 是否触及您的回撤红线 |
|---|---|---|---|
| -25bp | {DUR[-25]['approx_pct']:+.2f}% | {DUR[-25]['value_change_cny']:+,.0f} 元（按 1 亿示例） | ______ |
| +25bp | {DUR[25]['approx_pct']:+.2f}% | {DUR[25]['value_change_cny']:+,.0f} 元 | ______ |
| +50bp | {DUR[50]['approx_pct']:+.2f}% | {DUR[50]['value_change_cny']:+,.0f} 元 | ______ |
| +100bp | {DUR[100]['approx_pct']:+.2f}% | {DUR[100]['value_change_cny']:+,.0f} 元 | ______ |

> **口径提醒（必须同时给出）**
> ① 上表用教学假设参数（面值 1 亿、$D_{{mod}}$=5.0、凸性 30、平行移位），
> **不是任何真实产品的参数**；
> ② 未计入更高阶项、信用利差变动、税收与交易成本；
> ③ 若发生扭曲型变动（非平行移位），偏差方向不确定；
> ④ 这是估值近似，**不是收益预测，也不是回测业绩**。

## 四、什么证据会让我们改变判断

1. 10Y 收益率回升并突破 {ST[2]['y10']}%（{ST[2]['date']} 水平）→ 中枢下移判断需修正。
2. 曲线形态由平坦转为陡峭（或反之）且持续 → 平行移位假设失效。
3. 资金利率持续显著偏离政策利率 → 需要重新评估流动性环境。
4. 您的约束发生变化（久期上限调整、会计分类变更）→ 结论需重算。

## 五、需要您确认的信息

- [ ] 修正久期上限与当前实际久期
- [ ] 最大可接受回撤（金额或比例）
- [ ] 会计分类
- [ ] 流动性要求与对冲工具可用性

---

**材料性质声明**：本材料为个人研究产出的方法与模板示例，
不构成投资建议，不代表任何机构观点，也不包含任何真实产品参数或业绩数据。
"""

F["research/finance/04_fund_comparison_status.md"] = f"""# 固收基金可比研究 · 状态说明（未完成）

数据版本 {DV}。**本模块未完成，此处如实说明，而不是用替代指标凑数。**

## 为什么没做

基金层面的可比研究需要以下数据，本轮均未取得可靠来源：

| 需要的数据 | 为什么重要 | 本轮状态 |
|---|---|---|
| 基金份额类别（A/C/E 等） | 同一基金不同份额费率与净值不同，不能混比 | 未取得 |
| 可比区间（起止日） | 区间不同不可比 | 未取得 |
| 分红与拆分记录 | 影响净值序列连续性 | 未取得 |
| 管理费 / 托管费 / 销售服务费 | 直接影响净回报 | 未取得 |
| 定期报告披露滞后 | 持仓与策略披露有滞后，时点必须标注 | 未取得 |

## 明确不做的替代做法（以及为什么）

| 不能做 | 为什么 |
|---|---|
| 用收益率曲线变化当作基金收益 | 收益率下降带来的资本利得 ≠ 持有期总回报 |
| 用货币基金七日年化与债券基金区间收益直接排名 | 口径完全不同（年化 vs 区间、低风险 vs 信用/久期风险） |
| 给真实基金强行赋久期/凸性参数 | 公开披露不足以支撑，赋值即造假 |
| 用规模或评级代替风险指标 | 与本题「风险敞口」问题不相关 |

## 如果要做，需要什么

1. **来源**：基金管理人官网披露的定期报告与产品资料概要（法律文件）、
   中国证券投资基金业协会公开信息。
2. **口径规则（必须先定）**：同份额类别、同区间、含分红复权、
   费用口径统一（税前/税后）、披露时点标注。
3. **展示规则**：不同口径分别展示，不合并成一张排名表。
4. **必要限制**：过去净值不预示未来；区间选择会影响结论，
   必须同时给出至少两个区间。

## 替代交付

本轮用**修正久期情景工具**（`project-a.html` 第三节、打印版 P5）承担
「把风险翻译成客户能用的数字」这一功能。它的假设是明确标注的教学参数，
而**不是**给真实基金赋值——这是刻意的选择。
"""
# ============================================================ research/gtm
F["research/gtm/01_method_and_scope.md"] = f"""# 项目 B 研究方法与范围

数据版本 {DV}　生成 {GEN}

## 1. 范围（六个限制，不无限扩张）

| 维度 | 取值 | 理由 |
|---|---|---|
| 国家 | 英国（UK） | 单一国家；SMMT 口径含大不列颠与北爱尔兰 |
| 主车型 | BYD DOLPHIN SURF | 官方定位 *The Compact Electric City Car*，官网页续航 200 英里 |
| 直接竞品 | Renault 5 E-Tech electric / MG4 EV / Dacia Spring Electric | 见下方选择规则 |
| 优先用户 | 小型纯电车的车队与公司车决策链（对照私人首购） | 由渠道结构数据推出 |
| 验证切口 | 官方公开信息能否支撑同级别可比判断 | 信息缺口切口，可在独立研究者能力范围内验证 |
| 不做 | 多国对比、全车型矩阵、冒充品牌官方、代收线索、未授权联系达人 | 边界 |

## 2. 竞品选择规则（先定规则，再看数据）

1. 英国在售的紧凑型 / 小型纯电乘用车；
2. 品牌英国官网**可公开访问并核实规格**（先实测可达性）；
3. 覆盖价格阶梯：入门（Dacia Spring）— 主流（BYD DOLPHIN SURF）—
   主流偏上（Renault 5 / MG4 EV）。

**可达性实测结果（2026-09-11）**

| 车型/品牌 | 站点 | 结果 |
|---|---|---|
| BYD DOLPHIN SURF | byd.com/uk | 200 可达 |
| Renault 5 E-Tech | renault.co.uk | 200 可达 |
| MG4 EV | mg.co.uk | 200 可达 |
| Dacia Spring | dacia.co.uk | 200 可达 |
| Vauxhall Corsa Electric | vauxhall.co.uk | 连接失败 → 排除 |
| Citroën ë-C3 | citroen.co.uk | HTTP 403 → 排除 |
| Fiat 500e / Kia / Peugeot / Hyundai | — | 不可达或 403 → 排除 |

> **重要**：排除发生在**选择之前**，不是因为数据不利。
> 记录保留在 `market.json` 与 `src/common.py` 的 SOURCES 中，可复核。

## 3. 数据来源与口径

{{sources_note}}

## 4. 已知限制

1. SMMT 免费公开页**仅提供车型 Top10**。当月 BEV 榜中只有 `RENAULT 5`（736 辆），
   另两款竞品**无可公开核实的车型级注册量**。
2. BYD 英国官网的**价格与 PCP 月供为前端动态渲染**，静态采集不可得 →
   记为 `not_retrieved`，**留空不推测**。
3. 各品牌续航测试口径不一致（WLTP combined / urban 等）→ **不排名**。
4. 全市场渠道结构 ≠ 纯电细分 ≠ 该车型自身结构 → 已标注为外推限制。

## 5. 关键判断与失效条件

**判断**：{GX['insight']}

**失效条件**
{chr(10).join(f'{i+1}. {x}' for i, x in enumerate(GX['falsifiers']))}

**状态**：提出并准备验证；**未执行，无业务成效数据**。
""".replace("{sources_note}", "\n".join(
    f"- **{k}**｜{v['name']}｜权威性 {v['authority']}｜{v['url']}"
    for k, v in SOURCES.items() if v["region"] == "UK"))

F["research/gtm/02_competitor_baseline.md"] = f"""# 竞品基线（仅列官网可核实字段）

数据版本 {DV}。

> **使用纪律**：本表只并列展示各品牌英国官网公布的原始数值。
> 各品牌续航测试口径可能不同，**未对齐口径前不得据此排名**。

| 车型 | 角色 | 官方公布续航（英里） | 电池容量（kWh） | 官方来源 | 采集状态 |
|---|---|---|---|---|---|
{chr(10).join(
    "| " + c['model'] + " | " + ("研究对象" if c['is_subject'] else "直接竞品") + " | "
    + (" / ".join(c['range_miles']) if c['range_miles'] else "**未获取**") + " | "
    + (" / ".join(c['battery_kwh']) if c['battery_kwh'] else "**未获取**") + " | "
    + f"[官网]({c['source_url']})" + " | " + ("已核实" if c['retrieved_ok'] else "**失败**")
    + " |" for c in CMP)}

## 未获取的字段（不推测）

| 字段 | 状态 | 原因 |
|---|---|---|
| 官方售价 / PCP 月供 | 未获取 | BYD 英国官网由前端动态渲染，静态请求无法取得 |
| 首付、期限、尾款等金融条件 | 未获取 | 同上；金融方案必须同时记录这些条件才可比 |
| 车型级注册量（非 Top10） | 受限 | SMMT 免费公开页只提供 Top10 |
| 促销有效期 | 未获取 | 需人工查看官网活动页 |

## 英国市场基线（SMMT）

| 指标 | 数值 | 单位 | 期间 |
|---|---|---|---|
| 新乘用车注册（全部动力） | {GX['total_month']:,.0f} | 辆 | 最新月度 |
| 纯电 BEV 注册 | {GX['bev_month']:,.0f} | 辆 | 最新月度 |
| 纯电份额 | {PM[0]['share']} | % | 最新月度 |
| 纯电同比 | {PM[0]['yoy']:+.1f} | % | 最新月度 |
| 纯电注册（年初至今） | {GX['bev_ytd']:,.0f} | 辆 | 年初至今 |
| BYD 品牌注册（当月） | {BV['cur']:,.0f} | 辆 | 最新月度 |
| BYD 品牌注册（年初至今） | {BY['cur']:,.0f} | 辆 | 年初至今 |

## 渠道结构（本项目最有信息量的一组数）

| 渠道 | 数量（辆） | 份额 | 上年同期份额 | 同比 |
|---|---|---|---|---|
{chr(10).join(f"| {c['label']} | {c['units']:,.0f} | {c['share']}% | {c['share_prev']}% | {c['yoy']:+.1f}% |" for c in CH)}

**口径提醒**：分母为英国当月**全部**新乘用车注册，**不是**纯电细分，
**更不是** DOLPHIN SURF 自身的渠道结构。

## 当月 BEV 车型 Top10（SMMT 免费公开部分）

| 排名 | 车型 | 注册量（辆） |
|---|---|---|
{chr(10).join(f"| {m['rank']} | {m['model']} | {m['units']:,} |" for m in GX['bev_top10'])}

> 该榜中与本站竞品基线重合的只有 `RENAULT 5`。BYD 车型未进入当月前十。
"""

F["research/gtm/03_influencer_screening_method.md"] = f"""# 达人筛选方法（为什么本项目不做数值评分）

数据版本 {DV}。

## 结论先行

**本项目不产出任何数值化达人评分。** 这是刻意的取舍，不是遗漏。

## 原因

1. **平台条款未核实。** 用平台 API 数据生成「商业评分」属于衍生商业使用，
   主流平台（尤其 YouTube Data API）的服务条款对此有明确限制。
   在没有逐条核对清楚之前，我不做。
2. **评分要素不可得。** 一个有意义的评分离不开受众地域构成、真实互动率、
   报价与合作意愿。这些数据我没有，缺了它们评分就是噪声。
3. **不可核验的评分比没有评分更糟。** 一个看起来精确的数字会让人忘记它其实没有依据。

## 替代方案：透明的人工证据分层

只使用**公开可见、可人工复核**的信息，分三层：

| 层级 | 判定标准（全部需人工确认） | 用途 |
|---|---|---|
| 第一层：主题对口 | 公开作品主题与「英国小型纯电车 / 城市通勤 / 车队用车」相关 | 进入候选池 |
| 第二层：地区证据 | 作品或简介中有明确的英国地区线索（英国道路、牌照、地名、英式表达） | 保留在候选 |
| 第三层：合作形式可确认 | 公开可见的合作披露（如 clearly marked ad / gifted / 品牌合作说明） | 用于判断合作习惯 |

**明确保持未知的字段**（不填、不猜）：
- 受众地域构成（不做推断，英语内容 ≠ 英国受众）
- 报价与商务条件
- 转化率与历史效果
- 合作意愿

## 交付内容

- 候选清单（人工证据分层，不排序、不打分）
- 定制合作 brief（模板，含必须声明事项与禁止事项）
- 英文联系草稿（**仅作为草稿存在，未发送给任何人**）

## 需要人工完成的步骤

见 `interview/07_self_check_list.md` 的 C1：
逐个人工确认作品主题、地区证据与公开可见性，记录剔除理由。

## 若将来要做评分

必须同时满足：
1. 平台条款明确允许该类衍生使用；
2. 评分依据与权重全部公开，可被第三方复算；
3. 数据来源、采集时间、样本范围完整记录；
4. 明确标注不可得字段，不用推测值填补。
满足前三条之前，不做评分。
"""

F["research/gtm/04_two_week_plan.md"] = f"""# 两周执行计划（提出 / 准备验证 · 未执行）

数据版本 {DV}。**本计划未执行，所有成本数字为假设。**

## 目标

在一个可验证的切口上，检验「车队 / 公司车决策链的可比证据」这一假设是否成立。

**不是目标**：提升销量、降低获客成本、建立品牌合作。这些没有数据支撑，不做承诺。

## 假设与验证方式

| 项 | 内容 |
|---|---|
| 假设 | 面向英国车队 / 公司车决策链提供同级别可比证据，比面向私人消费者的大规模内容投放更有效 |
| 主要指标 | **外链点击**（定义为：独立研究页面上「查看品牌官网规格页」链接的点击次数） |
| 次要指标 | 页面停留时长、可比表下载次数 |
| **明确分开定义** | 外链点击 ≠ 试驾预约 ≠ 购车订单。三者分别记录，不合并、不换算 |
| 停止条件 | 连续 7 天外链点击为 0，或发现目标读者根本不是车队决策链 → 停止并修正假设 |

## 第一周

| 日 | 动作 | 负责人 | 依赖 | 成本假设 | 衡量方式 |
|---|---|---|---|---|---|
| D1 | 完成落地页文案定稿（英文，独立研究声明） | 我 | 竞品基线已核实 | 0 | 文档评审通过 |
| D2 | 落地页配置与部署（静态托管） | 我 | D1 | 0（用免费托管） | 页面可访问 |
| D3 | 对比表最终核对（口径警示必须显示） | 我 | 官方页面复核完成 | 0 | 逐字段核对记录 |
| D4 | 内容脚本定稿 + 拍摄/录制清单 | 我 | D1 | 假设 0（自录） | 脚本可读性检查 |
| D5 | 合作 brief 定稿（含必须声明事项） | 我 | 达人候选人工复核完成 | 0 | brief 评审通过 |
| D6 | 上线，配置点击统计 | 我 | D2、D3 | 0 | 统计可读到事件 |
| D7 | 第一周复盘：外链点击、停留时长 | 我 | D6 | 0 | 数据导出 |

## 第二周

| 日 | 动作 | 负责人 | 依赖 | 成本假设 | 衡量方式 |
|---|---|---|---|---|---|
| D8–D9 | 依据 D7 数据调整页面顺序与标题 | 我 | D7 | 0 | 版本记录 |
| D10 | 产出 2 条内容（面向车队用车的使用场景） | 我 | D4 | 假设 0（自产） | 发布记录 |
| D11 | 达人候选**人工**筛选与优先级记录（不评分） | 我 | brief 定稿 | 0 | 剔除理由记录 |
| D12 | 联系草稿定稿（**不发送**，除非我另行决定） | 我 | D11 | 0 | 草稿存档 |
| D13–D14 | 两周复盘：假设成立 / 不成立 / 证据不足 | 我 | 全部 | 0 | 复盘文档 |

## 成本假设

| 项目 | 假设金额 | 说明 |
|---|---|---|
| 域名 | 0 | 使用免费静态托管子域 |
| 托管 | 0 | 免费额度 |
| 内容制作 | 0 | 自录自剪 |
| 达人合作 | **未知** | 报价未获取，不做预算假设 |
| 时间投入 | 约 20–25 小时 | 我的时间成本，非现金支出 |

> 上表全部是**假设**。真实执行前需要重新确认，尤其「达人合作」一栏没有报价依据。

## 风险与停止条件

| 风险 | 触发信号 | 应对 |
|---|---|---|
| 假设不成立 | 7 天外链点击为 0 | 停止，转为验证私人渠道假设 |
| 页面被误认为品牌官方 | 出现相关反馈 | 立即加强独立研究声明，必要时下线 |
| 口径被质疑 | 有读者指出不可比 | 记录并修正对比表，公开修订说明 |
| 成本超预期 | 出现任何非零支出 | 暂停并回来确认预算 |

## 合规红线（不可越过）

- 不冒充品牌官方，不使用品牌商标作官方暗示
- 不代收品牌销售线索
- 不发送未经授权的联系
- 不把外链点击说成试驾预约或订单
- 不把本计划的「设计」说成「已取得的成效」
"""
for rel, content in F.items():
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content, encoding="utf-8")
    log_line(f"  写入 {rel}（{len(content)} 字符）")
