"""批量生成 interview/（续）与 research/ 下的交付物。数值均从 market.json 读取。"""
from __future__ import annotations

import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from common import ROOT, log_line                                   # noqa: E402

M = json.loads((ROOT / "github-upload" / "data" / "market.json")
               .read_text(encoding="utf-8"))
FX, GX = M["finance"]["derived"], M["gtm"]["derived"]
CUR, MM = FX["current"], FX["money_market"]
DUR = {r["shift_bp"]: r for r in FX["duration_scenario"]["rows"]}
ST = FX["stages"]
BV, BY = GX["byd"]["month"], GX["byd"]["ytd"]
CH, PM, CMP = GX["channels"], GX["powertrain_mix"], GX["competitors"]
DV = M["meta"]["data_version"]
GEN = M["meta"]["generated_at"]
FILES: dict[str, str] = {}

# ============================================================ interview 续
FILES["interview/03_STAR_and_review.md"] = f"""# STAR 与复盘

数据版本 {DV}。

> STAR 的 R（Result）在个人研究里最容易造假。本文件严格区分：
> **系统成果 / 研究成果 / 效率成果 / 业务成果**，没有业务成果就写「没有」。

---

## 项目 A

### S 背景（Situation）
国内债券收益率中枢持续下移。10Y 国债从 {ST[0]['date']} 的 {ST[0]['y10']}%
降到 {CUR['date']} 的 {CUR['y10']}%。下行行情里，久期带来的资本利得空间在收窄，
但组合承担的久期风险敞口并未同步下降。同时我看到：同一个市场判断，
不同约束（久期上限、回撤容忍、会计分类）的机构客户受到的影响差别很大，
而常见的沟通材料往往只讲观点、不讲「在你的约束下意味着多少回撤」。

### T 任务（Task）
用公开可得的数据把曲线与资金面的现状做成可复核的基线，
并把「久期风险」量化成不同约束客户能直接使用的数字。
范围自定：只做少数核心指标，不做全市场覆盖。

### A 行动（Action）
1. **先做数据可得性验证再定范围**。逐一实测中债、中国货币网、人民银行的公开接口，
   确认可用参数（这些参数公开文档并未给出，是实测出来的）。
2. **自建采集管道**：5 个来源，增量更新、缓存去重；
   抓取失败保留上次有效结果并标记过期，不写零。
3. **守口径纪律**：国债与地方政府债曲线不混用；DR 与 FR 分列；
   到期收益率与持有期回报分开表述。
4. **做三阶段曲线复盘**，识别出中枢下移这一趋势。
5. **建修正久期情景工具**：dP/P ≈ -D_mod·dy + 0.5·C·dy²，并单独说明凸性修正的量级。
6. **写反证与失效条件**，把客户案例明确标注为教学假设。

### R 结果（Result）
- **系统成果**：13 条数据记录，每条带定义、单位、统计范围、期间、抓取时间、来源编号、验证状态；
  程序可重复运行；34 项自动检查通过。
- **研究成果**：识别出收益率中枢下移；量化了 +100bp / -100bp 情景下
  教学组合的估值变动（{DUR[100]['approx_pct']:+.2f}% / {DUR[-100]['approx_pct']:+.2f}%）；
  明确了凸性修正的量级（±100bp 内约占久期效应的 24%）。
- **效率成果**：首轮建设约 2 小时（含接口勘察）；单次增量更新约 1–2 分钟。
  **这是程序流程耗时，不是人力节省百分比**——我没有人工基线，所以不编百分比。
- **业务成果**：**没有**。这是个人研究，没有客户、没有组合、没有投资业绩。

### 复盘（Review）
- **做得对的**：先验证数据可得性再定范围。这一步避免了把研究范围定在拿不到的数据上。
- **做得不够的**：固收基金可比研究是最后才确认不可得的，导致一个模块中途降级。
  应该在一开始就问「基金层面的份额、区间、分红、费用、披露滞后有没有可靠来源」。
- **下次会改的**：久期情景应该更早同时给出一阶线性与含凸性的结果对比，
  因为向非技术客户解释「为什么亏损小于久期相乘」时，对比比公式有效。

---

## 项目 B

### S 背景（Situation）
中国车企在英国电动车市场的存在感上升，BYD 品牌注册量同比接近翻倍
（当月 {BV['cur']:,.0f} 辆，同比 {BV['pct_change']:+.1f}%）。
但作为独立研究者，我发现一个结构性障碍：
**公开数据存在，但不可比**。注册量有官方口径，规格散落在各品牌官网，
价格由前端动态渲染拿不到。

### T 任务（Task）
限定一个国家、一款车型、约三款直接竞品、一类优先用户和一个可验证的切口，
完成市场监测、竞品基线、痛点分析、达人筛选与执行材料。

### A 行动（Action）
1. **先定竞品选择规则，再实测可达性**：英国在售紧凑/小型纯电 + 官网可核实 +
   覆盖价格阶梯。Vauxhall 与 Citroën 站点实测不可达，
   在**选择之前**排除，并保留记录作为证据。
2. **搭 SMMT 增量采集**，覆盖动力类型、销售渠道、品牌级、车型 Top10 四个维度。
3. **严格区分口径**：注册量/销量/交付量、品牌级/车型级、纯电/插混、英国/大不列颠。
4. **识别切口**：从渠道结构（车队 {CH[0]['share']}% vs 私人 {CH[1]['share']}%）
   推出「优先准备车队与公司车适配证据」。
5. **产出执行材料**：英文独立研究落地页、对比表、内容脚本、合作 brief、两周计划。
6. **主动放弃不可靠做法**：达人数值评分、评论挖掘、AI 准确率。

### R 结果（Result）
- **系统成果**：SMMT 采集成功，覆盖 59 个品牌 + 车型 Top10；4 款车型官网规格已核实；
  来源可达性逐一实测并记录（含不可达）。
- **研究成果**：量化了英国纯电市场基线（{GX['bev_month']:,.0f} 辆 / {PM[0]['share']}% 份额）
  与渠道结构；产出一个带三条失效条件的 GTM 切口判断。
- **效率成果**：首轮建设约 2 小时；单次更新约 1–2 分钟。
- **用户/市场成果**：**没有**。没有投放、没有流量数据、没有线索、没有订单。
  达人候选是**人工证据分层的清单，不是合作**。

### 复盘（Review）
- **做得对的**：主动放弃。放弃达人数值评分、放弃价格对比、放弃评论挖掘。
  这三件事都能让交付物「看起来更满」，但都会引入不可靠或不合规的内容。
- **做得不够的**：价格是 GTM 对比的核心变量，我应该在研究设计阶段就决定
  「用浏览器渲染采集」还是「明确放弃」。现在的处理（明确放弃并留空）是正确的结果，
  但决策点偏晚。
- **下次会改的**：用户痛点部分应先确认平台内容分析条款，再决定是否投入样本采集。
  本轮因未确认而放弃，是刻意取舍，但流程顺序可以更前置。

---

## 一句话总结（面试可以直接用）

> 这两个项目里我最想证明的不是「我会用工具」，而是
> **我知道什么时候该停下来**——数据拿不到就写拿不到，
> 口径不通就不并表，条款没确认就不做分析，没执行就不写成效。
"""

FILES["interview/04_questions_20.md"] = f"""# 20 道深入追问与参考答法

数据版本 {DV}。参考答法是**要点提示**，不是标准答案——如果你有更好的说法请替换。

---

## 数据来源与溯源类

**Q1. 你的数据从哪来？能现场演示取数吗？**
> 5 个固收来源 + 5 个英国来源，全部在 `src/common.py` 的 SOURCES 登记表里，
> 有编号、URL、接口、权威性分级和再分发说明。
> 现场演示：跑 `python src/finance_fetch.py`，几十秒就能重新拉一遍。
> 曲线取数日 {CUR['date']}，10Y 是 {CUR['y10']}%——如果我改了这个数，网站、图表、PPT 会同时不对，
> 因为它们读的是同一份 market.json。

**Q2. 你怎么知道这个数是权威的？**
> 分三级：A 是原始披露或官方机构（中债、人民银行、品牌官网）；
> B 是官方衍生或行业机构（SMMT）；C 是媒体二手。
> 我的结论**只用 A 和 B**，没有用 C 级来源。
> 但我要主动说：**全部是单一权威来源，尚未独立交叉验证**。
> 因为同源转载不算独立验证，而我没有第二个独立来源。

**Q3. 中债的接口你是从哪找到的？**
> 公开文档没有给出可用参数。我是打开页面看它的前端请求，实测出来的。
> 具体来说：曲线的最新值是 POST 到 `czbChartSearch`，
> 历史某一天用 `czbQueryXy` 且 `qxmc=1` 才是国债曲线（`qxmc=2` 是地方政府债，
> 差一点就把两条曲线拼在一起了）。这个过程记录在 `checks/pipeline_fixups.md`。

**Q4. 你的数据多久更新一次？**
> 分节奏：金融数据按交易日与官方发布时间；竞品页面每日检查；
> 注册与宏观数据随发布更新；用户材料与达人名单按周或有新证据时更新。
> 自动化任务设为北京时间每天 24:00 跑一次，但**只在有重要变化、失败或需要处理时提醒**，
> 无变化只记录检查状态。

**Q5. 如果来源挂了怎么办？**
> 保留上次有效结果并标记过期，不写零。
> 这件事不是设想——本机有本地代理（127.0.0.1:64265），会对回环和部分外网请求
> 间歇性返回 502，我在建设期就遇到了。所以程序加了 curl 回退通道和重试，
> 并且区分四种状态：数据未发布 / 来源不可访问 / 校验失败 / 无变化。

---

## 口径与概念类（最容易翻车的地方）

**Q6. 期限和久期有什么区别？**
> 期限是到期时间；久期是价格对利率的敏感度。10 年期国债的修正久期通常在 8 左右，
> 不是 10。我项目里所有久期数字都明确是「修正久期」，并且是假设参数。

**Q7. 收益率下降，你的组合是不是就赚钱了？**
> **不能这么说。** 收益率下降带来的价格变化是资本利得的一部分，
> 持有期总回报还包含票息与再投资，还要扣交易成本和税收。
> 我**没有做收益回测**，因为缺少可靠的总回报数据。
> 项目里所有组合数字都是教学情景，不是真实产品业绩。

**Q8. 你用的 DR007 和 FR007 是一回事吗？**
> 不是。DR（我这里用的 FDR 系列）是存款类机构质押式回购利率，FR 是全市场。
> 两者参与机构范围不同。我在页面上把它们**分列展示**，不合并成一条序列。

**Q9. 10Y-2Y 利差 0.44 个百分点，这个数怎么来的？**
> 在同一条国债曲线上，同日取 10Y 和 2Y 的到期收益率相减。
> 当前是 {CUR['y10']}% - {CUR['y2']}% = {CUR['spread_10y2y']}。
> 关键前提是**同一条曲线**——如果 10Y 用国债、2Y 用国开债，这个数就没有意义。

**Q10. 注册量是销量吗？**
> 不是。SMMT 统计的是英国新乘用车**注册**。销量、交付量、注册量的统计主体和时点都不同。
> 我在每个涉及注册量的图和表下面都写了这句提醒。

**Q11. BYD 当月 3,867 辆，那 DOLPHIN SURF 卖了多少？**
> **我不知道，而且我不能从品牌数据推出来。**
> {BV['cur']:,.0f} 辆是 BYD 品牌全部动力类型的注册量，包含 DM-i 混动和其他车型。
> SMMT 免费公开页只给车型 Top10，DOLPHIN SURF 不在当月的 BEV 前十，
> 所以我没有它的车型级数据。这是我认为必须承认的限制。

**Q12. 你的续航对比图是不是在排名？**
> 不是，而且我明确写了「未对齐口径前不得排名」。
> 各品牌官网的续航表述方式不同（WLTP combined / urban 等），我也没有逐一对齐。
> 这张图的用途是展示「公开信息不可比」这个事实本身，而不是比较性能。

---

## 方法、判断与反证类

**Q13. 「优先做车队渠道」这个判断是怎么来的？**
> 从渠道结构数据：当月英国新乘用车注册里车队占 {CH[0]['share']}%、
> 私人占 {CH[1]['share']}%。车队是更大的池子，且公司车有税务结构上的驱动。
> 但我要立刻补三条失效条件：一是如果这款车的决策其实由零售主导，判断要下调；
> 二是渠道占比是**全市场**口径，不是纯电细分，也不是这个车型自己的结构；
> 三是注册量不等于销量。所以这只是一个**待验证的切口优先级建议**。

**Q14. 那你验证了这个判断吗？**
> **没有。** 我做的是「提出并准备验证」，不是「已验证」。
> 我准备了英文落地页、对比表、内容脚本、合作 brief 和两周计划，
> 但两周计划里的衡量方式和停止条件是设计，不是结果。
> 没有投放，所以没有流量、没有线索、没有订单。

**Q15. 什么证据会让你改变判断？**
> 三件事：①这款车的经销网络如果以零售为主，说明私人渠道才是主战场；
> ②如果能拿到纯电细分的渠道结构，而且和全市场不同，我会改用细分口径；
> ③如果能拿到车型级注册量，我会直接检验它和渠道结构的关系，而不是靠推断。

**Q16. 久期情景里为什么正负不对称？**
> 因为凸性。公式是 dP/P ≈ -D_mod·dy + 0.5·C·dy²，第二项 dy² 恒为非负，
> 所以它永远在「减损」——损失比线性估计小，收益比线性估计大。
> 具体量级：凸性项在 ±25bp 内约占久期效应的 6%，±100bp 内升到约 24%。
> **这也是为什么移位越大，线性估计越不可靠。**

**Q17. 你的情景假设平行移位，现实中会发生吗？**
> 很少完全平行。现实里更常见的是扭曲型变动（steepener / flattener）。
> 所以我把它写成假设、并且写进失效条件：如果是扭曲型变动，
> 我的情景会系统性高估或低估，方向都不确定。
> 用这个工具的前提是先接受平行移位这个简化。

---

## 执行、合规与个人贡献类

**Q18. 达人你是怎么选的？有报价和转化率吗？**
> 我**没有做数值化评分**，而且这是刻意的。
> 原因是平台条款：YouTube API 等明确限制用其数据做衍生商业评估，
> 我没有核实清楚之前不做。
> 我用的是透明的人工证据分层：只看公开可见的作品主题、地区证据、合作形式。
> **受众地域、报价、转化率、合作意愿一律保持未知**——我不知道，就不填。
> 也没有联系过任何人，英文联系稿只是草稿。

**Q19. 这个项目你花了多少时间？AI 帮你省了多少？**
> 首轮建设含接口勘察与调参大约 2 小时；之后每次增量更新 1–2 分钟。
> **但我不报「节省 xx%」**，因为我没有做人工基线测量。
> 我能给的对比是**程序流程对比**：手工做同样的事需要每天打开几个网站抄数、
> 手工制图，而这个流程是自动跑完的。
> 另外要说清楚：AI 自动任务启动程序本身也会消耗额度，不是零 token。

**Q20. 这里面哪些是你做的，哪些是 AI 做的？**
> 分工我写成了表，在 `interview/05_contribution_evidence.md`。
> 简短说：**研究问题、范围决策、口径规则、判断与失效条件、复核标准**是我定的；
> **代码编写、数据解析、制图、文档排版**由我指挥 AI 完成，我再验证。
> 我不能声称「全部我手写代码」——我的角色是设计者、审阅者和最终责任人。
> 而且有几件事我明确标为**待我本人完成**：抽查来源原文、手算久期情景、
> 复核达人候选、真实读者测试。这些没有做完就不会标成完成。
"""

FILES["interview/05_contribution_evidence.md"] = f"""# 个人贡献与证据对应表

数据版本 {DV}。

> **这张表的用法**：面试被问「这是你做的还是 AI 做的」时，直接照这个说。
> 诚实的分工描述比含糊的「我做的」更可信。

---

## 一、角色分工

| 环节 | 我（刘柏廷） | AI 辅助 | 证据位置 |
|---|---|---|---|
| 研究问题定义 | **我定**：为什么做、要回答什么 | 无 | 本文档、`project-a.html` 第一节 |
| 范围决策 | **我定**：只做 5 个固收来源；项目 B 锁死为一国一车三竞品 | 提供可达性实测结果供我决策 | `research/gtm/method_and_scope.md` |
| 来源筛选与权威性分级 | **我定**：A/B/C 分级，结论只用 A/B | 执行可达性测试 | `src/common.py` SOURCES |
| 口径规则 | **我定**：不混用曲线、DR/FR 分列、注册量≠销量 | 无 | `evidence.html` 口径对照表 |
| 关键判断 | **我定**：中枢下移、久期保护减弱、客户差异在约束、车队优先 | 提供数据与图表 | `market.json` claims（K-01…K-08） |
| 失效条件 | **我定**：每条判断都要写「什么证据会推翻它」 | 无 | 各项目页与打印版 |
| 数据采集代码 | 我提出需求与验收标准 | **AI 编写** | `src/finance_fetch.py`、`src/gtm_fetch.py` |
| 数据解析与清洗 | 我验证结果 | **AI 编写** | `src/build_all.py` |
| 计算公式 | **我确认公式与假设**（久期/凸性、bp 换算） | AI 实现与制图 | `src/build_all.py` duration_scenario |
| 图表设计 | **我定规范**（单位、期间、来源、限制必须上图） | AI 实现样式 | `exports/charts/` |
| 网站实现 | **我定结构与交互需求** | **AI 编写** | `github-upload/` |
| PPT / PDF | **我定页序与内容** | AI 排版生成 | `slides/`、`print/` |
| 人工复核 | **我负责（当前待完成）** | 不代做 | `interview/07_self_check_list.md` |

---

## 二、我做的、可核验的动作

| # | 动作 | 证据 |
|---|---|---|
| 1 | 决定把固收采集范围收窄到 5 个来源，理由是「宁可少而可核实」 | `interview/02_scripts_30s_2min_5min.md` |
| 2 | 要求所有数据记录带验证状态字段，并区分 verified / single_authoritative / computed / unverified / failed | `market.json` → `datasets[].verified` |
| 3 | 要求程序在抓取失败时保留上次有效值并标记过期，禁止写零 | `src/common.py` `get_or_keep()` |
| 4 | 要求在竞品选择**之前**完成官网可达性实测，并保留不可达记录 | `market.json` → `gtm.not_retrieved`、`SOURCES` 中的 S-VX-01 / S-CT-01 |
| 5 | 决定放弃达人数值化评分，理由涉及平台条款 | `research/gtm/influencer_screening_method.md` |
| 6 | 决定放弃价格对比而非用推测值填空 | `project-b.html`、`market.json` → not_retrieved |
| 7 | 决定把「车队优先」写成「提出并准备验证」而非成果 | `interview/03_STAR_and_review.md` |
| 8 | 要求每个重要判断必须附失效条件 | `market.json` → `claims[].exception` |
| 9 | 要求网站、图片、PPT、PDF 从同一份数据生成 | `src/build_all.py` → `market.json` → 四个出口 |
| 10 | 要求逐页渲染检查 PDF、测算 PPT 文字溢出 | `checks/verify_print.mjs`、`checks/verify_pptx.py` |

---

## 三、AI 完成、我已验证的部分

| 项目 | 验证方式 | 结果 |
|---|---|---|
| 采集程序可重复运行 | 多次运行 `finance_fetch.py` / `gtm_fetch.py` | 通过（含失败降级实测） |
| 数值非空、单位齐备、版本一致 | `checks/validation.json` 34 项自动检查 | 全通过 |
| 网站渲染与数据注入 | `checks/verify_site.mjs`（Playwright） | 5 页、0 未绑定、0 破图、0 控制台错误 |
| 打印版逐页溢出 | `checks/verify_print.mjs` | 14 页、0 溢出 |
| PPT 文字溢出与数值一致性 | `checks/verify_pptx.py` | 18 项全通过 |
| 图表分辨率 | 程序读取 PNG 头部 | 3840×2160 / 2480×3507 @300ppi |

---

## 四、**我本人尚未完成**（不得标为已完成）

| # | 事项 | 为什么不能由 AI 代做 |
|---|---|---|
| 1 | 抽查 3–5 条来源原文，逐条核对数值 | 「我核验过」只有我自己做才成立 |
| 2 | 手算一次久期情景（±25bp / ±100bp），与程序结果比对 | 这是「我理解公式」的唯一证据 |
| 3 | 复核竞品官网规格页面 | AI 抓取可能受页面改版影响 |
| 4 | 复核达人候选的公开信息 | 涉及对具体个人的判断，必须人工 |
| 5 | 邀请 2–3 位读者完成三项查找任务测试 | AI 模拟用户不能替代真实用户 |
| 6 | 把「口径待确认」的实习百分比数字核实清楚 | 涉及真实履历，不能推测 |
| 7 | 确认个人信息（邮箱、学位、毕业时间） | 不能编造 |
| 8 | 在实际打印机上试打 | 屏幕检查不等于纸面实打 |

清单与验收标准见 `interview/07_self_check_list.md`。

---

## 五、不能说的话（红线）

- ✗「我独立完成了全部代码」→ 应说「我负责设计与验收，代码由 AI 辅助编写」
- ✗「提升了品牌销量 / 降低了获客成本」→ 无业务数据
- ✗「与达人 / 品牌合作」→ 不存在
- ✗「AI 分类准确率 xx%」→ 无人工标注留出集
- ✗「建立了投资组合并取得收益」→ 久期情景是教学假设
- ✗「自动化已稳定运行很久」→ 只验证过一次真实定时触发，应如实说明
"""

FILES["interview/06_reverse_explain.md"] = f"""# 反向讲解：从一张图追溯回源头

数据版本 {DV}。

> **练习方式**：面试官指着任意一张图问「这个数怎么来的」，
> 你要能在 60 秒内一路答到源页面。这份文件就是把这条路径写出来。
> 请**自己动手走一遍**，不要只读。

---

## 例 1：从「10Y-2Y 期限利差 {CUR['spread_10y2y']}」往回追

| 层 | 内容 |
|---|---|
| 你看到 | 图 C-04 / 打印版 P4 表格：10Y-2Y 利差 = {CUR['spread_10y2y']} 个百分点 |
| 数据记录 | `D-FIN-SPREAD-10Y2Y`，verified = `computed`（不是来源直接给的） |
| 计算式 | 同一曲线同日：{CUR['y10']}%（10Y）− {CUR['y2']}%（2Y）= {CUR['spread_10y2y']} |
| 原始数据 | `D-FIN-YC-10.0Y` 与 `D-FIN-YC-2.0Y`，均来自 `S-CB-01` |
| 接口 | POST `https://yield.chinabond.com.cn/cbweb-czb-web/czb/czbChartSearch` |
| 源页面 | `https://yield.chinabond.com.cn/cbweb-czb-web/czb/moreInfo?locale=cn_ZH&qxId=ycqx` |
| 口径 | 中债国债收益率曲线（银行间市场，到期收益率），取数日 {CUR['date']} |
| 陷阱 | 若 10Y 取国债、2Y 取地方政府债（`qxmc=2`），这个数就错。必须同曲线同源。 |

**如果被问「为什么不用国开债曲线」**：口径不同，绝对水平和利差都不一样。
本项目的所有利差都固定在同一条国债曲线上计算，跨曲线不拼接。

---

## 例 2：从「教学情景 +100bp → {DUR[100]['approx_pct']:+.2f}%」往回追

| 层 | 内容 |
|---|---|
| 你看到 | 图 C-03 / 打印版 P5：+100bp 时组合估值 {DUR[100]['approx_pct']:+.2f}% |
| 数据记录 | `duration_scenario.rows[shift_bp=100]`，verified = `computed` |
| 公式 | dP/P ≈ -D_mod·dy + 0.5·C·dy² |
| 代入 | D_mod = 5.0，C = 30，dy = 100bp = 0.0100 |
| 分步 | 久期项 = -5.0×0.01 = -5.00%；凸性项 = 0.5×30×0.0001 = +0.15%；合计 = -4.85% |
| 金额 | 面值 100,000,000 × (-4.85%) = {DUR[100]['value_change_cny']:+,.0f} 元 |
| 性质 | **教学假设参数**，不是任何真实基金或产品的参数 |
| 陷阱 | ①不能把 -4.85% 说成「真实组合亏损」；②不能把 dy 写成 100（那是 10000bp）；③凸性项恒非负 |

**如果被问「为什么凸性项是正的」**：dy² 永远是正数，所以 0.5·C·dy² 永远 ≥ 0，
它让损失变小、收益变大。这也是凸性「对持有人有利」的说法来源。

---

## 例 3：从「车队渠道 {CH[0]['share']}%」往回追

| 层 | 内容 |
|---|---|
| 你看到 | 图 C-06 / 打印版 P8：车队 {CH[0]['share']}%，私人 {CH[1]['share']}% |
| 数据记录 | `D-GTM-CH-FLEET-SHARE` 等，verified = `single_authoritative` |
| 源页面 | `https://www.smmt.co.uk/vehicle-data/car-registrations/` |
| 解析方式 | HTML 表格解析（该页是「整表一行」结构，需按标签重新分组，不能按行取值） |
| 分母 | 英国当月**全部新乘用车注册**（含所有动力类型），总量 {GX['total_month']:,.0f} 辆 |
| 口径 | 注册量，不是销量、不是交付量 |
| 陷阱 | 这是**全市场**口径，不是纯电细分，也不是 DOLPHIN SURF 自身的渠道结构 |

**如果被问「凭什么用它推 DOLPHIN SURF 的策略」**：
严格说，**不能直接推**。我的用法是「它给出一个值得优先验证的假设」，
而且我在页面上写了三条失效条件。这是我把它标为「解释 / 建议」而不是「事实」的原因。

---

## 例 4：从「BYD 当月 {BV['cur']:,.0f} 辆」往回追

| 层 | 内容 |
|---|---|
| 你看到 | 图 C-07 / 打印版 P9 |
| 数据记录 | `D-GTM-BYD-M`，来源 `S-SM-02` |
| 源页面 | SMMT 品牌注册量表（当月列） |
| 口径 | BYD **品牌**、**全部动力类型**、英国新乘用车**注册** |
| 对照 | 上年同期 {BV['prev']:,.0f} 辆；同比 {BV['pct_change']:+.1f}% |
| **关键限制** | **品牌级 ≠ 车型级**。它包含 DM-i 混动与其他车型，不能当作 DOLPHIN SURF 销量。 |
| 诚实说法 | 「BYD 品牌在英国增长很快，但我无法从公开免费数据里拆出 DOLPHIN SURF 单车型的量。」 |

---

## 自测：请你自己完成一次

1. 打开 `github-upload/data/market.json`，任选一条 `datasets[]`，说出它的
   来源编号、口径、期间、验证状态。
2. 打开 `https://www.chinabond.com.cn` 或 `yield.chinabond.com.cn`，
   找到当日国债 10Y 收益率，和我记录的 {CUR['y10']}% 比对（注意日期是否为工作日）。
3. 打开 SMMT 的注册数据页，手动核对车队与私人占比是否等于
   {CH[0]['share']}% / {CH[1]['share']}%。
4. 用计算器手算 dy = 0.0025（+25bp）、D=5.0、C=30 的结果，
   与程序输出的 {DUR[25]['approx_pct']:+.2f}% 比对。

做完这四步，你就具备了回答任何溯源追问的能力。**做完前请在本文件末尾标注未完成。**
"""
for rel, content in FILES.items():
    p = ROOT / rel
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(content, encoding="utf-8")
    log_line(f"  写入 {rel}（{len(content)} 字符）")
